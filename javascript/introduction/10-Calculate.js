// 10. Calculate the average of 3 numbers.

let num1 = 100;
let num2 = 200;
let num3 = 500;

// Here, the values ​​were added together and divided by 3 (the total number of values).
let res = (num1 + num2 + num3) / 3;

// show res
console.log(res);