// 2. Declare variables of different types (string, number, boolean).

// declare variables types only using his own type
let str = 'This is a string';
let num = 10;
let bool = true;

// show all
console.log(str, num, bool);