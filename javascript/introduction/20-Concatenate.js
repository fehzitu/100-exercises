// 20. Concatenate two strings.

let str1 = 'Hello';
let str2 = 'World!';

// show res
console.log(`${str1}, ${str2}`);
// When we use `` (craze) we can then concatenate variables within strings, just pass the variable within a variable block "${variable_here}"